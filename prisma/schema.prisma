generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model cinema {
  id           String  @id @db.Uuid


  @@index([id], map: "idx_cinema_cinemaid")
}
